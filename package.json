{
  "name": "images-to-deflate-tiff",
  "version": "0.0.0",
  "description": "Generate a deflate-compressed tiff file from one or more images.",
  "private": true,
  "license": "WTFPL",
  "repository": "luncheon/images-to-deflate-tiff",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "files": ["index.d.ts"],
  "scripts": {
    "build": "tsc -p . && biome lint",
    "watch": "tsc -p . -w --preserveWatchOutput"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.6.3"
  }
}
